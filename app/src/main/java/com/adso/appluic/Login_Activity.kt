package com.adso.appluic

class Login_Activity
{
    //val toggleButtonShowPassword = findViewById<ToggleButton>(R.id.toggleButtonShowPassword)
    //val editTextPassword = findViewById<EditText>(R.id.editTextPassword)

    //toggleButtonShowPassword.setOnCheckedChangeListener { buttonView, isChecked ->
    // if (isChecked) {
            // Mostrar contraseña
    //        editTextPassword.inputType = InputType.TYPE_TEXT_VARIATION_VISIBLE_PASSWORD
    //   } else {
            // Ocultar contraseña
    //       editTextPassword.inputType = InputType.TYPE_CLASS_TEXT or InputType.TYPE_TEXT_VARIATION_PASSWORD
    //    }
        // Mover el cursor al final del texto después de cambiar el tipo de entrada
    //   editTextPassword.setSelection(editTextPassword.text.length)
    // }

}